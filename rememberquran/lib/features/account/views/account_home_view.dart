import 'package:flutter/material.dart';

class AccountHomeView extends StatelessWidget {
  const AccountHomeView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('AccountHomeView')),
      body: const Center(child: Text('AccountHomeView is under construction')),
    );
  }
}
